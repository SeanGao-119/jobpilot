# Use a server-side PostgreSQL connection string only.
# Local development can use Supabase session pooler (:5432).
# For Vercel/serverless deployment, prefer transaction pooler (:6543).
DATABASE_URL=postgresql://postgres.PROJECT_REF:CHANGE_ME@aws-0-REGION.pooler.supabase.com:5432/postgres
